# 본문 이미지

아래 파일을 이 폴더에 넣어 주세요.

```
02.png ~ 09.png
```

- **01.png** — 사용 안 함 (메인 썸네일은 `output/thumbnails/` 에서 자동 생성)
- **02~06** — 본문 구간마다 순서대로 삽입
- **07~09** — 삽입 후 `config/image-manifest.json` 의 `linkUrl` 링크 연결

링크 URL은 샘플 블로그 배너 링크에 맞게 `image-manifest.json` 에서 수정하세요.
